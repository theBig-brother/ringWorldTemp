       val selectBox = VisSelectBox<String>()
        val options = com.badlogic.gdx.utils.Array<String>()
        options.addAll("Option 1", "Option 2", "Option 3", "Option 4")

        selectBox.items = options  // 设置下拉菜单的选项
        // 设置下拉菜单的默认值
        selectBox.selected = options[0]
        // 设置下拉菜单的位置
//        selectBox.setPosition(100f, 300f)
